/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // Set the basePath for GitHub Pages.
  // Assumes the repository name is 'gemini-newcomer-site'.
  // If your repository name is different, please update this value.
  basePath: '/gemini-newcomer-site/',
};

export default nextConfig;
